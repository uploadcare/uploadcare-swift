name: test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Package update
      run: swift package update
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
